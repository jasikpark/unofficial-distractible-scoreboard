<header>
  <h1>
    <a href="/">
      <span class="unofficial">Unofficial</span>
      <img
        class="distractible"
        src="/DISTRACTABLE-Logo.png"
        alt="Distractible"
      />
      <span class="scoreboard">Scoreboard</span>
    </a>
  </h1>
</header>

<style>
  header {
    font-family: var(--font-default);
    font-weight: bold;

    text-transform: uppercase;

    color: var(--color-white);
    background-color: var(--color-black);
  }

  h1 a {
    font-size: var(--font-size-5, 36px);
    margin-block-start: 0;
    margin-block-end: 0;
    padding-block-start: 1rem;
    padding-block-end: 1rem;
    display: grid;
    grid-template-areas:
      "unofficial"
      "distractible"
      "scoreboard";
    align-items: center;
    justify-items: center;
  }
  img {
    width: 100%;
  }
  span {
    width: max-content;
  }
  .unofficial {
    color: var(--color-green);
    grid-area: unofficial;
  }
  .distractible {
    grid-area: distractible;
  }
  .scoreboard {
    color: var(--color-purple);
    grid-area: scoreboard;
  }
</style>
