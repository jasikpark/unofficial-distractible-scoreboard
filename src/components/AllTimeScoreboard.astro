---
import { Score } from "@src/episodes.ts";
import AllTimeScore from "@components/AllTimeScore.astro";
export interface Props {
  scores: Score[];
}
const { scores = [] } = Astro.props as Props;
---

<section>
  <h1>All Time Scores</h1>
  <ul>
    {scores.map( (score, placement) => <AllTimeScore score={score} placement={placement} /> )}
  </ul>
</section>

<style>
  section {
    color: var(--color-white);
    background-color: var(--color-black);

    font-size: var(--font-size-5);
  }

  section > * + * {
    padding-top: 1rem;
  }

  h1,
  ul {
    text-align: center;
    margin-left: auto;
    margin-right: auto;
  }
</style>
